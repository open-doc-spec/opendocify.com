---
// FILE: changelog.astro
// Purpose: Release changelog page for ODS.
// Layer: Marketing page

import Layout from "../layouts/Layout.astro";
import { REPO_URL } from "../lib/releases";
import { changelog } from "../data/changelog";

function withInlineCode(text: string) {
  return text.split(/(`[^`]+`)/g).map((part) => {
    if (part.startsWith("`") && part.endsWith("`")) {
      return { code: true, value: part.slice(1, -1) };
    }
    return { code: false, value: part };
  });
}
---

<Layout title="Changelog: Open Document Spec (ODS)" activeNav="changelog">
  <div class="page-container">
    <div class="header-section">
      <div class="pill-badge">ods CLI</div>
      <h1 class="page-title">Changelog</h1>
      <p class="page-subtitle">
        What changed in the <code>ods</code> command — so you know what to type, what to fix in your files, and when to update.
      </p>
    </div>

    <div class="timeline">
      {changelog.map((entry) => (
        <article class="timeline-item">
          <div class="timeline-meta">
            <span class="version-tag">{entry.tag}</span>
            {entry.latest && <span class="latest-tag">Latest</span>}
            <span class="date">{entry.date}</span>
          </div>
          <div class="timeline-content">
            <h2>
              {withInlineCode(entry.title).map((part) => (part.code ? <code>{part.value}</code> : part.value))}
            </h2>
            <p>
              {withInlineCode(entry.summary).map((part) => (part.code ? <code>{part.value}</code> : part.value))}
            </p>
            <ul>
              {entry.items.map((item) => (
                <li>
                  <strong>{item.label}</strong>
                  {': '}
                  {withInlineCode(item.text).map((part) => (part.code ? <code>{part.value}</code> : part.value))}
                </li>
              ))}
            </ul>
          </div>
        </article>
      ))}
    </div>

    <div class="changelog-cta">
      <p class="changelog-note">
        Sourced from the <code>ods</code> engine changelog. GitHub still has every tag, including internal CI builds.
      </p>
      <a href={`${REPO_URL}/releases`} target="_blank" rel="noopener noreferrer" class="cta-button">
        Download a release →
      </a>
    </div>
  </div>
</Layout>

<style>
  .page-container {
    width: min(92vw, 900px);
    margin: 0 auto;
    padding-top: var(--space-48);
    padding-bottom: var(--space-64);
  }

  .header-section {
    text-align: center;
    margin-bottom: var(--space-48);
  }

  .pill-badge {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    padding: var(--space-4) var(--space-12);
    background: var(--card-bg);
    border: var(--border);
    border-radius: var(--radius-pill);
    color: var(--fg-muted);
    margin-bottom: var(--space-16);
  }

  .page-title {
    font-size: clamp(2.4rem, 5.2vw, 3.4rem);
    font-weight: 700;
    line-height: 1.12;
    letter-spacing: -0.032em;
    margin-bottom: var(--space-16);
    color: var(--fg);
  }

  .page-subtitle {
    font-size: 1.15rem;
    font-weight: 500;
    color: var(--fg-muted);
    max-width: 680px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .timeline {
    border-left: var(--border);
    padding-left: var(--space-32);
    margin-left: var(--space-16);
    margin-bottom: var(--space-64);
  }

  .timeline-item {
    position: relative;
    margin-bottom: var(--space-48);
  }

  .timeline-item::before {
    content: "";
    position: absolute;
    left: calc(-2rem - 5px);
    top: 6px;
    width: 9px;
    height: 9px;
    border-radius: 50%;
    background: var(--fg);
  }

  .timeline-meta {
    display: flex;
    align-items: center;
    gap: var(--space-12);
    margin-bottom: var(--space-12);
  }

  .version-tag {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 700;
    padding: var(--space-4) var(--space-8);
    background: var(--action-bg);
    color: var(--action-fg);
    border-radius: var(--radius-sm);
  }

  .latest-tag {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    padding: var(--space-4) var(--space-8);
    border-radius: var(--radius-sm);
    background: var(--ods-accent-soft, #e6f4ea);
    color: #15803d;
  }

  .date {
    font-size: 0.8rem;
    color: var(--fg-dim);
  }

  .timeline-content h2 {
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--fg);
    margin-bottom: var(--space-8);
  }

  .timeline-content h2 code,
  .page-subtitle code,
  .timeline-content p code,
  .timeline-content li code {
    font-family: var(--font-mono);
    font-size: 0.9em;
  }

  .header-section .page-subtitle code {
    font-size: 0.95em;
  }

  .timeline-content p {
    font-size: 0.875rem;
    color: var(--fg-muted);
    line-height: 1.6;
    margin-bottom: var(--space-16);
  }

  .timeline-content ul {
    list-style-type: disc;
    padding-left: var(--space-24);
  }

  .timeline-content li {
    font-size: 0.85rem;
    color: var(--fg-muted);
    margin-bottom: var(--space-8);
    line-height: 1.5;
  }

  .timeline-content li strong {
    color: var(--fg);
  }

  .changelog-cta {
    text-align: center;
  }

  .changelog-note {
    max-width: 36rem;
    margin: 0 auto var(--space-16);
    font-size: 0.85rem;
    line-height: 1.5;
    color: var(--fg-muted);
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    padding: var(--space-12) var(--space-24);
    background: var(--action-bg);
    color: var(--action-fg);
    border-radius: var(--radius-pill);
    font-weight: 600;
    font-family: var(--font-mono);
    font-size: 0.875rem;
    transition: opacity 0.2s ease;
  }

  .cta-button:hover {
    opacity: 0.9;
  }

  @media (max-width: 640px) {
    .timeline-item {
      grid-template-columns: 1fr;
      gap: var(--space-12);
    }
    .timeline-meta {
      flex-direction: row;
      align-items: center;
      gap: var(--space-8);
    }
    .main {
      padding: var(--space-32) var(--space-16) var(--space-48);
    }
  }
</style>
