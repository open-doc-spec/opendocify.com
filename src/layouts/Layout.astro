---
import BaseLayout from './BaseLayout.astro';
interface Props { title?: string; description?: string; activeNav?: string; }
const { title = 'Open Document Spec (ODS) — precise context for AI', description, activeNav = 'home' } = Astro.props;
---
<BaseLayout title={title} description={description} activeNav={activeNav}><slot /></BaseLayout>
